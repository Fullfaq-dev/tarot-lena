services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
    volumes:
      - media_data:/app/backend/static/generated
      - ./backend:/app/backend:ro
      - ./prompts:/app/prompts:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    command: python -m app.workers.runner
    restart: unless-stopped
    volumes:
      - ./backend:/app/backend:ro
      - ./prompts:/app/prompts:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  bot:
    profiles: ["polling"]
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    command: python -m app.bot.polling
    restart: unless-stopped
    volumes:
      - media_data:/app/backend/static/generated
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  admin:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./frontend-admin/dist:/usr/share/nginx/html:ro
      - ./frontend-admin/nginx.conf:/etc/nginx/conf.d/default.conf:ro

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./site:/usr/share/nginx/site:ro
      - ./deploy/nginx.active.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - certbot_www:/var/www/certbot:ro
    depends_on:
      - api
      - admin

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: tarot
      POSTGRES_USER: tarot
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tarot}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tarot -d tarot"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped

volumes:
  postgres_data:
  media_data:
  certbot_www:
